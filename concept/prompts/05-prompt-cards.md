Use case: text-localization
Asset type: targeted correction to a landscape pixel-art game card screen, exactly 1536x1024
Input images: Image 1 is the edit target from the first generation.
Primary request: change only the top green header so it reads exactly "THE HUMAN IS PROMPT ENGINEERING". The two-letter word after HUMAN must be the letters I then S: "IS", not "I5" and not any other characters.
Typography: preserve the existing uppercase green blocky monospace pixel font, size, spacing, position, glow, and single-line layout.
Constraints: modify only those two mistaken header glyphs. Keep every other pixel-art element unchanged: all three card frames, their borders and highlight state, all three illustrations, all card titles, all effect text, both buttons, background, palette, pixel density, dithering, composition, and 1536x1024 landscape dimensions. Do not rewrite, move, restyle, add, or remove anything else. No extra words, symbols, labels, logos, or watermark.
